# 实现任务：表格单元格颜色映射模式

## 阶段 1：基础

- [x] 1.1 新增映射模式枚举与同步选项
  - 创建 `TableCellColorMappingMode` 枚举（Background/Text）
  - 扩展 `SyncOptions` 增加该设置，默认 `Background`
  - **依赖**：无
  - **需求**：R1.2

- [x] 1.2 在本地配置中持久化映射模式
  - 增加配置字段（string 或 int）保存 `TableCellColorMappingMode`
  - 读取：缺失时默认 `Background`
  - 保存：持久化用户选择
  - **依赖**：1.1
  - **需求**：R1.3

## 阶段 2：界面

- [x] 2.1 在 `panelConfig` 增加下拉选择
  - 添加 Label + ComboBox
  - 选项：“映射为单元格背景”“映射为字体颜色”
  - 变更时更新配置与当前 `SyncOptions`
  - **依赖**：1.2
  - **需求**：R1.1

## 阶段 3：映射逻辑

- [x] 3.1 扩展背景色映射辅助方法
  - 实现 `*_background` 注释色的映射助手
  - 不支持时回退 `default`
  - **依赖**：1.1
  - **需求**：R3.3

- [x] 3.2 在 Notion 表格映射中应用模式
  - 使用 `TableBlock.CellRows` 访问单元格背景元数据
  - 背景模式：使用背景色注释
  - 字体模式：保持现有行为
  - 背景色优先于前景色
  - **依赖**：3.1
  - **需求**：R2.1, R2.2, R2.3, R2.4

- [x] 3.3 处理有背景的空单元格
  - 插入不可见占位以承载背景色
  - 确保不引入可见字符
  - **依赖**：3.2
  - **需求**：R2.5

## 阶段 4：验证

- [ ] 4.1 手动同步验证
  - 验证表头背景、数据行背景、前景+背景混合、空单元格背景
  - 切换设置并重新同步，确认模式切换生效
  - **依赖**：3.3
  - **需求**：R2.1, R2.2, R2.5, R3.1

- [ ] 4.2 API 调用数量验证
  - 对比同步前后日志/统计，确保 Notion API 调用数量不显著增加
  - **依赖**：3.3
  - **需求**：R3.2
